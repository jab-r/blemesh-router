// Top-level build file. Plugin versions here match ../loxation-android so the
// ported mesh code compiles against the same AGP + Kotlin toolchain.
plugins {
    id("com.android.application") version "8.13.1" apply false
    id("org.jetbrains.kotlin.android") version "2.2.20" apply false
}
